<UserControl x:Class="Pms.ManageWorkspaces.UI.Controls.ProgressControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Canvas x:Name="canvas2" Height="100" Width="100" Visibility="Collapsed"> 
            <Canvas x:Name="canvas1" Height="100" Width="100" RenderTransformOrigin=".5,.5">
                <Canvas.RenderTransform>
                    <RotateTransform x:Name="spin" Angle="0">
                        
                    </RotateTransform>
                </Canvas.RenderTransform>
            </Canvas>
            <Label HorizontalContentAlignment="Center" VerticalAlignment="Center" Height="32" Canvas.Left="26" Canvas.Top="36">Loading</Label>
        </Canvas>
    </Grid>
</UserControl>
